using System.Collections.Generic;
using DragonWar.Lib.Database;

public static class DB
{
    /*
    private static Dictionary<DatabaseType, DatabaseManager> Managers { get; set; } 
    [InitializerMethod]
    public static bool initializ()
    {
        return false;
    }


    private static bool GetDBManagerByType(DatabaseType pType,out DatabaseManager pManager)
    {
        if (Managers.TryGetValue(pType, out pManager))
            return true;

        return false;
    }*/

    

}

